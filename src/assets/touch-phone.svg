<svg viewBox="110 70 180 270" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <filter id="shadow" x="-20%" y="-20%" width="140%" height="140%">
      <feGaussianBlur in="SourceAlpha" stdDeviation="6" />
      <feOffset dx="0" dy="8" />
      <feComponentTransfer>
        <feFuncA type="linear" slope="0.1" />
      </feComponentTransfer>
      <feMerge>
        <feMergeNode />
        <feMergeNode in="SourceGraphic" />
      </feMerge>
    </filter>

    <style>
      @keyframes finger-slide {
        0% { transform: translate(120px, 150px); opacity: 0; }
        20% { transform: translate(0, 0); opacity: 0.6; }
        40%, 60% { transform: translate(0, 0) scale(0.85); opacity: 0.8; }
        80% { transform: translate(0, 0); opacity: 0; }
        100% { transform: translate(120px, 150px); opacity: 0; }
      }

      @keyframes btn-press {
        0%, 35%, 65%, 100% { transform: scale(1); }
        45%, 55% { transform: scale(0.92); }
      }

      @keyframes check-pop {
        0%, 45% { transform: scale(0); opacity: 0; }
        60%, 100% { transform: scale(1); opacity: 1; }
      }

      @keyframes ripple-out {
        0%, 45% { transform: scale(0.5); opacity: 0; }
        50% { opacity: 0.3; }
        75%, 100% { transform: scale(2); opacity: 0; }
      }

      .touch-point { transform-origin: 200px 260px; animation: finger-slide 3s infinite cubic-bezier(0.45, 0.05, 0.55, 0.95); }
      .btn-unit { transform-origin: 200px 260px; animation: btn-press 3s infinite ease-in-out; }
      .check-icon { transform-origin: 200px 260px; animation: check-pop 3s infinite cubic-bezier(0.17, 0.67, 0.83, 0.67); }
      .ripple { transform-origin: 200px 260px; animation: ripple-out 3s infinite ease-out; }
    </style>
  </defs>

  <g filter="url(#shadow)">
    <rect x="135" y="80" width="130" height="240" rx="32" fill="#FFFFFF" />
    <rect x="142" y="87" width="116" height="226" rx="26" fill="#FBFBFD" />
  </g>

  <rect x="180" y="98" width="40" height="10" rx="5" fill="#E5E5EA" />
  <rect x="165" y="140" width="70" height="6" rx="3" fill="#E5E5EA" opacity="0.6" />
  <rect x="165" y="155" width="40" height="6" rx="3" fill="#E5E5EA" opacity="0.3" />
  <circle class="ripple" cx="200" cy="260" r="25" fill="#007AFF" />

  <g class="btn-unit">
    <rect x="155" y="235" width="90" height="50" rx="25" fill="#007AFF" />
    <path class="check-icon" d="M188 260 l8 8 l14 -14" stroke="white" stroke-width="4" fill="none" stroke-linecap="round" stroke-linejoin="round" />
  </g>

  <g class="touch-point">
    <circle cx="200" cy="260" r="24" fill="#007AFF" opacity="0.5" />
    <circle cx="200" cy="260" r="12" fill="white" opacity="0.4" />
  </g>
</svg>
