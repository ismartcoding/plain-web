/** Maps to CSS `-webkit-text-stroke` */
export interface TextStroke {
  width: number
  color: string
}

/** Maps to CSS `text-shadow: offsetX offsetY blur color` */
export interface TextShadow {
  offsetX: number
  offsetY: number
  blur: number
  color: string
}
